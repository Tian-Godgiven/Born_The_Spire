[toc]

# 任务列表

## 进行中 (In Progress)

### 第一层地图完整体验（堆料+验证流程）

目标: 将第一层地图的内容填充完整，跑通从开始到 Boss 的完整流程

内容储备已就绪: 遗物 / 普通战斗 / 精英 / Boss 敌人 / 12 个事件全部实现完毕
  现行事件在 `src/static/list/room/event/eventList.ts`
  备胎池 3 个（苦行僧 / 深井 / 双胞胎）未采用，未来扩池或换血时备用
  mod 侧的复刻（大鱼 / 换脸商 / 金神像）**不进核心池**，只作 mod 系统验证样例
  竞技场（`sts_event_colosseum`）暂不注册，两场嵌入战斗还没定

待完成: 端到端流程验证
  测试环境已就绪（2026-08-06）:
    初始配置清空：playerList 只留心脏，无初始卡牌 / 遗物 / 药水
    三张 999 测试卡（秒杀 / 永生 / 无敌）由测试模式注入初始牌组，标 pool:["test"] 不进奖励池；关掉则开局 3 打击 + 3 防御。控制台 `toggleTestMode()` 切换，下次开始游戏生效
    设置面板可跳过动画 / 调速，重复跑流程时能省掉演出等待

## 本轮口述（按顺序做，先别开工）

### 0.1.1 还没做完的

疫孢菌母改版：血量 90。感染打击 3 伤 + 1 毒，挂菌网根系。菌网对对手上毒时抽 1（每回合 3 次，2 级 5 次），3 级锻感染打击为 0 费 3 伤 2 毒。不上回合毒。出牌：常规四拍爆发→蔓延→感染或硬化×2；≤30% 狂暴双卡招（爆发+蔓延，随后三拍蔓延+硬化）。孢子 1 费，打出消耗、留着手上毒后虚无。3 级菌盖 `drop: false` 已接。寄生菌丝 / 菌丝蔓延已改。剧毒核心只留施加中毒 +1。中毒体系要重做。详见 `文档/参考/玩家友好设计.md`。
  腐化铠甲拿掉，改挂 3 级菌盖且 `drop: false`（开战 5 甲，不进同化）。菌盖补第 3 档：L1 回合开始 3 甲 + 硬化 6；L2 硬化 8；L3 甲改为 5。Boss 不打硬化。`drop: false` 已接到敌人 organ 条目和奖励收集。

自走焚烧炉不攻击：fallback 写成 `intent: "unknown"` 把攻击藏成迷雾；点火核 `how: "via"` 叠不上点火（useCard 的 medium 是卡，持有者是 source）。已改成 moves 循环铁刺→熔铸打击→护壁，第1回合铸铁力、点火≥4 重铸插队；点火核改为 `how: "make"`。混合 tag 兜底不再优先基础防御。
卡面力量/选敌预览：Card.vue 已走 `previewCardEffects`，但 `getStatusDescribe` 先 `toRaw` 再读 Status，代理被剥掉。已改成类型判断用 toRaw、读数字走 `.value`。瞄准敌人时 hoverTarget 也传给 DescribeText。
  一层打完：floor_1 `nextFloors` 清空，Boss 后进通关页「未完待续」（感谢 / Demo 只有第一层 / QQ 群 / 欢迎反馈）。本局统计这屏不放，以后单独做。
  小兽交互：盯上的那行悬停右侧出「喂养」；点了立刻给它、好感 +1。霸占的那份领不了。前进仍没领也算出喂。战利品行用通用插件口（`rewardRowAddonRegistry` + `Reward.blockClaim`），小兽 UI 在 `BeastLootAddon`，RewardModal 不再认识小兽。
  黑市整体重排版：仍在 0.1.1 清单里。

### 新口述

重锤伤害：故障机器的重击现行 23（升级 28），玩家 50 血，抱怨太高。稍稍下调。卡：`enemy_card_heavy_strike`。

治疗无人机（维修无人机上的急救营养液 `enemy_organ_emergency_battery`）：
  现行：受伤后生命低于 30% 回 20，charges 2，升级到 3 级改回 35。
  改成：生命低于 5 点时立即获得 5 治疗；如果这一下直接打死，不触发。
  升级 1：阈值改成生命低于 5%。
  升级 2：治疗改为 7。
  仍有次数限制（「全局实用次数」按现行 charges 那套，整局共用，待动手时核对是每场还是整局）。

水池文案：现行「一个宁静的休息处...」改成「你在废墟中间发现了一处紫黑的水池，很熟悉的味道」。`PoolRoom.vue`。

黑市出售：售卖器官、售卖物质/生命单独一页，可和购买页切换。黑市只会随机选 3 个持有器官收购，不让玩家自己挑。现行是左侧出售区和弹窗里自选器官。

本局统计：通关/失败页现在只有到达层数、经过房间，Demo 通关页先不展示。以后做成真正的本局数据（造成伤害、受到伤害等），通关和失败都能看。现行 `RunStats` / `RunStatsPanel`。

## 待处理 (TODO)

### 已处理（工厂去留另议）

启动 `Cannot access 'Chara' before initialization` 已修。工厂要不要长期保留另说。

根因
    `factories.ts` 顶层值导入 `Player` / `Enemy`。preload 加载 `Target` 时碰到工厂，会提前执行 `class Player extends Chara`。

修法
    `createPlayer` / `createEnemy` 在函数里再取类。`createOrgan` 等调用处不动。
    `Card.ts` 对 `Target` 改为 `import type`。

详见《常见错误排查手册》L1

### 高优先级

1. **进阶系统对玩家开放**
   内容: 引擎和 `ascensionList` 已有 0–10 档。选人页 / 顶栏已用 `ASCENSION_UI_ENABLED` 藏掉，开局强制进阶 0。内容稳定后打开开关，并把器官精通、金神像等进阶变体一起接上。

2. **遗物/器官主动能力UI提示**
   内容:
     - 遗物/器官的冷却时间/计数器的显示
     - 可用遗物/器官的可用提示和暂时不可用提示和完全不可用提示

3. **角标样式调整**
   内容:
     - 角标统一配置系统已实现，但视觉样式还需要调整优化
     - 调整角标的大小、颜色、位置、字体等视觉表现

4. **黑市正式测试**
   内容: 对黑市系统进行完整的功能测试，发现并修复问题

5. **水池正式测试**
   内容: 对水池系统进行完整的功能测试，发现并修复问题

6. **水池功能完善**
   内容: 完善水池房间的各项功能，确保所有水池行动正常工作

7. **器官质量系统完善**
   内容: 完善器官质量相关的系统功能

8. **事件富文本系统**
   内容: 事件描述支持富文本效果，允许 mod 作者使用标签控制文本表现
     - 颜色、粗细、抖动等视觉效果
     - 声音联动
     - 自定义标签语法，如 `<抖动>恐~惧~吧~</抖动>`
     - 支持调用 Vue 组件渲染复杂效果

9. **CardSelectReward 补充功能**（主体已完成，等具体场景需要时再补）
   - 卡牌池筛选辅助（稀有度/tag/来自哪个器官），目前由调用方自己提供 cardOptions
   - 卡片富渲染（当前只显 label + description，跟 relic 弹窗一致水平），
     等宝箱事件跑起来看效果再决定要不要用 `<Card>` 组件

### 中优先级

1. **主动使用系统完善**
   文件: `src/ui/hooks/interaction/abilityMenu.ts`
   待实现:
     - 实现能力详情弹窗 (第141行)
     - 实现不可用原因提示 (第149行)

2. **敌人行为系统完善**
   文件: `src/core/objects/system/EnemyBehavior.ts:169`
   内容: 实现 getStateStacks 方法

3. **玩家头顶本回合受伤清单**（未做）
   显示本回合可能挨到的伤害包，不要合成一个「总计」数字（护甲、击杀、行动顺序会让合计骗人）。
   每条独立读取声明，不跑假战斗：
     敌人攻击意图 — 已有 `previewEffect` 折叠
     中毒 — 读当前层数（回合开始扣血）
     敌人「回合结束造成 N 点伤害」— 读 reaction 里写死的 damage 值
   随机（过期隔板）和「打完才知道」的不写进清单，或标成可能。
   参考本次预览收口：`文档/子系统/UI/效果预览系统.md`

4. **奖励系统完善**
   文件:
     - `src/core/objects/reward/RelicReward.ts:57` - 将遗物添加到玩家
     - `src/core/objects/reward/GoldReward.ts:33` - 实现金钱系统后添加金钱
     - `src/core/objects/room/TreasureRoom.ts:127` - 等金钱系统实现后创建 GoldReward

### 低优先级

1. **黑市系统完善**
    待实现:
      - 器官/药水/卡牌也改用 drawItem + 栏位配置（目前仅遗物已改造）

3. **房间生成系统完善**
    文件:
      - `src/core/objects/room/roomChoice.ts:72` - 根据层级和概率规则生成房间
      - `src/core/objects/system/roomSelection/IncrementalProbabilityRule.ts:103` - 通过roomRegistry判断类型
      - `src/static/registry/initGameEventRegistry.ts:20` - 注册其他事件类型

4. **地图系统完善**
    文件:
      - `src/core/objects/room/FloorSelectRoom.ts:176` - 显示地图UI
      - `src/core/objects/system/FloorManager.ts:427,506` - 传入完整的Room对象历史
      - `src/core/objects/system/roomSelection/FloorSelectionState.ts:142,160` - 序列化和恢复动态规则配置

5. **机制系统完善**
    文件: `src/static/registry/mechanismRegistry.ts:355`
    内容: 持续回合数和自定义清零条件

6. **事件系统完善**
    文件:
      - `src/static/list/room/event/eventList.ts:111,145,156` - 打开卡牌/器官选择界面
      - `src/static/list/room/event/eventEffectMap.ts:24,32,370` - 实现材料和随机奖励

7. **意图系统完善**
    文件: `src/core/objects/system/Intent.ts:208`
    内容: 传入实际的目标，调整cardsToIntent参数

8. **层级配置完善**
    文件: `src/static/list/floor/floorList.ts:22,24`
    内容: 添加Boss战斗和黑市房间

9. **游戏统计系统**
    内容: 埋点收集本局游戏数据，用于失败/通关页面展示
      - 击杀敌人数、获得金币总数、使用卡牌次数、本局用时等
    相关: RunStats.ts, RunStatsPanel.vue 已搭好框架，等数据就位后扩展

10. **卡牌数值参数：命名统一 + 消除双写**
    背景: 卡牌 status 上的数值参数（`vulnerable:2` / `stacks:2` / `vulnStacks:1` 几种写法并存）
          目前只被 describe 读取，实际效果值在 effects.params 里另外硬编码一份，
          改一处不改另一处会导致卡面与实际效果不符
    内容:
      - 命名统一为 `<stateKey>Stacks`（规范已写入实现参考手册，新内容按此写）
      - effects.params 改用 `$item.status(xxx)` 引用，让 status 成为数值的单一来源
    范围: cardList.ts 约 35 张卡 + mods/ 下的卡牌；目前仅 `cardList.ts:1015` 一张做到了单一来源

11. **事件/触发器链 SMT 别名映射系统**
    背景: 在多层事件/触发器传递过程中，source/medium/target 的含义随每层变化，导致 mod 作者难以追踪"原始对象是谁"
    内容:
      - 方案A：在 trigger 层定义 smtAliases 别名映射
      - 方案B：扩展 targetType 增加 triggerEventSource/triggerEventTarget/triggerEventMedium

## 待讨论 (Pending Discussion)

- **黑市卡牌移除服务** — 付费从牌组移除卡牌，参考杀戮尖塔，机制待定
- **黑市对话系统** — 黑市老板在不同交互场景下有对话反馈（如买不起时辱骂玩家），需要对话系统支撑，当前先用 message 提示占位
- **复刻杀戮尖塔原版事件** — 有空时用现有事件系统复刻更多原版事件，验证系统完备性并补充第一层事件池。已完成换脸商(Face Trader) + 5 面具遗物，过程中扩展了 roomEnter/openChest 事件、$event.info() 条件语法、emptyChest/gainRelicFromPool 效果

## 文档整理

### 未归档文档 (待分类)

1. `主动使用系统.md` - 需要与代码同步
2. `卡牌引用系统.md` - 需要与代码同步
3. `效果预览系统.md` - 已与 previewEffect 折叠对齐
4. `器官解锁与精成系统.md` - 需要与代码同步
5. `器官解锁与精成系统-实现文档.md` - 需要与代码同步
6. `进阶系统.md` - 需要与代码同步

### 可归档文档

1. `机制UI系统使用指南.md`
2. `机制系统Mechanism.md`
3. `种子随机数系统.md`
4. `如何在新效果中使用随机数.md`
5. `discoverCard效果的随机数生成原理.md`
6. `黑市系统配置指南.md`
7. `器官稀有度与标签系统.md`
8. `器官质量系统与操作方法.md`
9. `器官奖励动作系统.md`
10. `器官奖励动作系统-快速参考.md`
11. `水池行动扩展系统.md`
12. `水池行动扩展系统-快速参考.md`

## 备忘事项

ifShow 目前是函数类型，未来如果使用场景增多，考虑改为 Condition 系统（需要解决 sceneData 引用命名问题）

进阶系统相关数值变体（待进阶系统实现后调整）:
- 金神像事件：砸烂 进阶15+ 失去35%生命（基础25%），躲藏 进阶15+ 失去10%最大生命（基础8%）

来自 `文档/备忘.md`:
- 【器官→脑】抽牌数量提升/牌效果为重抽or加抽/能够得知敌人的行为的强度
- 【器官→眼】能够获知敌人的行动/行动类型
- 根据交互的itemTarget判断选择目标的行为
- 词条也用触发器来做
- 核心问题：导致状态层数减少的对象就是它自己
- 状态对象问题：所有效果都要通过事件触发，否则不会进事件栈
- 战斗行为定义: 回合开始(抽牌、恢复能量)、回合结束(弃牌、耗光能量)
