[toc]

# 任务列表

## 进行中 (In Progress)

### 第一层地图完整体验（堆料+验证流程）

目标: 将第一层地图的内容填充完整，跑通从开始到 Boss 的完整流程

内容状态（2026-07-29）:
  遗物：Boss 5/5 + Shop 8 + Common/Uncommon/Rare 均已设计 ✅
  普通战斗：够用（用户确认） ✅
  精英：够用（先用着，不重设计） ✅
  Boss 敌人：3 个已实现 ✅
  事件：目标 12 种，数据草稿全部落地 ✅（等技术前置解锁后进入实现）
    已入 eventList.ts：神秘商人 / 宝箱 / 治疗泉水 / 湖面精灵 / 无常之神 / 胚胎发育 / 废弃神龛 / 献祭祭坛（草）/ 孵化室（草）/ 饲主（草）/ 移植手术（草）/ 收藏家（草）
    "（草）" 项都已带 TODO 前置块，等各自实现前置解决后才能跑通
    8 个新原创方向全部敲定 ✅（2026-07-29 数据草稿全部入库）
    详见 `文档/设计/第一层事件设计.md`
    备胎池 3 个（苦行僧 / 深井 / 双胞胎）未采用，未来扩池或换血时备用
    mod 侧的 4 个复刻（大鱼 / 竞技场 / 换脸商 / 金神像）**不进核心池**，只作 mod 系统验证样例

待完成:
  1. 前置：CardSelectReward（详见待处理高优先级第 8 项）
  2. 逐个实现 8 个新事件，接入楼层配置（设计已完成，等 CardSelectReward）
     献祭祭坛：✅ 前置全齐（2026-07-29），可 playtest
       a. ✅ 秀色可餐 / 浅尝辄止 两遗物已入 relicList.ts（`original_relic_delectable_feast` / `original_relic_shallow_taste`）
       b. ✅ ifAble 语法：新增 `organCount()` accessor（EntityAccessor.ts）
       c. ✅ 事件专属池标记：直接用 pool: ["exclusive"]（沿用无常之神的祝福模式）
       d. ✅ "用满 N 场后禁用"通路：沿用无常之神的祝福模板（battles-remaining + disabled + condition 三件套）
     PS：ifAble 顺带解锁了孵化室/饲主/收藏家（都用 organCount()）和移植手术（也用了 organsWithCardsCount()）
     孵化室：✅ 前置全齐（2026-07-29，除 e 外），可 playtest
       a. ✅ event effect gainState（eventEffectMap.ts）：留作通用工具，孵化室本身改用遗物形态所以未用上
       b. ✅ 孵化中的胚胎 遗物（event_relic_incubating_embryo）：改为遗物形态存在
          原因：Player.startBattle() 会 initState() 清空所有状态，无法跨战斗持有；沿用无常之神的祝福 battles-remaining/disabled 模板 + accumulateAndTrigger 作纯累计器（threshold: 999999，跳过 maxTriggerPerBattle 触发的重置逻辑）实现 damage-taken 跨战斗累计
          结算阈值：≤30 → 共生之种；>30 → 饥饿之种（原设计 50，用户改 30）
       c. ✅ 卡牌「寄生」（original_card_parasite，诅咒 tag，cannot-play）已入 cardList.ts；被移除时 -3 最大生命通路已接入（见 e）
       d. ✅ 遗物「共生之种」「饥饿之种」（event_relic_symbiote_seed / event_relic_hungry_seed）已入 relicList.ts；饥饿之种 possess.effects 里塞入 1 张寄生
       e. ✅ 「寄生」卡的"被移除时触发"通路（2026-07-30）：Card.setOwner 加了通用 possess.triggers 挂载块（对齐遗物的 possess 通路）；寄生数据用 how:"via" 监听 removeCard，reaction 走 addMaxHealthAndHeal value:-3。转化事件项目里目前不存在，未处理（若将来加 transformCard 事件再补一条 trigger 即可）
     饲主的实现前置（数据草稿 2026-07-29 已入 eventList.ts）:
       a. 新增遗物「饥饿的怪物」（大系统）：战斗奖励占用（让/抢/强制占据）+ 好感度阶段解锁（3/6/9 三阶）
          a1. ✅ 基底 + 战斗奖励占用（2026-07-30）：event_relic_hungry_beast 入 relicList.ts（rarity:"rare" + pool:["exclusive"]，counter badge 显好感度）；possess.effects 走 hungryBeast_registerInterceptor（新 EffectFunc /core/effects/relic/hungryBeastInterceptor.ts）挂 owner 的 after take beforeShowRewards：randomInt 挑一份 reward，弹让/抢；未喂≥3 时 forced 单钮强制占据；yield → splice + favor+1 + refuse=0，grab → refuse+1；eventList.ts 饲主 gainRelic 的 relicKey 修正为 event_relic_hungry_beast
          a2. ✅ ≥3 陪睡（2026-07-30）：新增 restHeal 事件通路（PoolRoom drink + healHealth eventEffect 都从 heal 改为 restHeal，effectUnits 仍是 heal effect）；遗物 possess.triggers 挂 owner 的 after take restHeal，condition `$source.status(hungry-beast-favor) >= 3`，reaction petHeal 走 heal value:10。走的是 declarative reaction 模式，未新加 EffectFunc（用户在此拦下"为啥要专门一个 effect key"）
          a3. ✅ ≥6 共战（2026-07-30）：遗物 possess.triggers 加第二条 `after take turnEnd`，condition `$source.status(hungry-beast-favor) >= 6`，action coFightStrike；reaction 走 targetType:"allEnemies.random" + damage value:5。全 declarative，无新 EffectFunc
          a4. ✅ ≥9 挡刀（2026-07-30）：遗物 possess.triggers 追加 before take dead（condition favor>=9 && blocked-once==0）action petBlock；reaction cancelCurrentEvent + heal 1 + setBaseStatus blocked-once=1；再加 after take battleStart action resetPetBlock 每场清零。全 declarative，复用金铸头骨/瓶中精灵那套 [[reference_revive_pattern]]
       b. ✅ 让/抢 UI 模态（2026-07-30）：rewardIntercept.ts（promise 化 hook，InterceptChoice: yield/grab）+ RewardInterceptModal.vue（2px 黑边白底极简，正常 2 键/强制 1 键）+ Running.vue 挂载。约定：yield=让给拦截者（调用方 splice reward），grab=抢过来（保留），forced 仅 UI 单钮返回 yield
       b0. ✅ beforeShowRewards 事件通路（2026-07-30）：BattleRoom 抽 finalizeAndShowRewards 方法，四个 handleXxxRewards 末尾 doEvent(beforeShowRewards) + await endTransaction 让触发器有机会 splice/push info.rewards（也可整体换引用）；触发器允许 async（如让/抢 UI 弹窗）
       b1. ✅ Status 承载字段（2026-07-30）：statusMap.ts 加 hungry-beast-favor（好感度，≥3/6/9 三阶）/ hungry-beast-refuse-count（未喂计数，≥3 强制占据）/ hungry-beast-blocked-once（本场已挡刀标记，battleStart 由遗物 possess.triggers 清零）三个 hidden/notNegative/special 字段，都挂在遗物身上
       c. ✅ 挡刀致命伤拦截通路已在 a4 内落地（复用金铸头骨/瓶中精灵那套，未新加基础设施）
     收藏家：✅ 前置全齐（2026-07-29），可 playtest
       a. ✅ NPC 点名 pickCollectorTarget（eventList.ts 顶部内嵌 helper，按 rare>uncommon>common 稀有度优先 + 同层随机）
       b. ✅ removeOrganByKey event effect（eventEffectMap.ts）：可通过 { key: "removeOrganByKey", params: { organKey } } 定向移除
       c. ✅ 讨价还价掷骰分支：扩展 customCallback 支持返回 nextScene 字符串（EventMapData.ts + EventRoom.ts），haggle 内 randomChance(0.5) → 返回 haggle_win/haggle_lose
       d. ✅ 事件级 onEnter 钩子（EventMapData.ts + EventRoom.ts）+ 场景 description 支持函数式（已存在）：进入时预 pick 存 sceneData，main 场景描述读 pickedOrgan.label 展示"他盯上了 X"
     移植手术的实现前置（数据草稿 2026-07-29 已入 eventList.ts）:
       a. 两阶段"选卡 + 选目标器官"UI 组件（选卡时按器官分组显示，选完卡再选目标器官）
       b. ContentModifier "改归属"通路：把卡从器官 A 的 ContentModifier 撤销，加到器官 B 的 ContentModifier（保持卡效果不变）
       c. ✅ ifAble 语法：organCount() + organsWithCardsCount() 已加
  3. 端到端流程验证

## 待处理 (TODO)

### 高优先级

1. **遗物/器官主动能力UI提示**
   内容:
     - 遗物/器官的冷却时间/计数器的显示
     - 可用遗物/器官的可用提示和暂时不可用提示和完全不可用提示

2. **角标样式调整**
   内容:
     - 角标统一配置系统已实现，但视觉样式还需要调整优化
     - 调整角标的大小、颜色、位置、字体等视觉表现

3. **黑市正式测试**
   内容: 对黑市系统进行完整的功能测试，发现并修复问题

4. **水池正式测试**
   内容: 对水池系统进行完整的功能测试，发现并修复问题

5. **水池功能完善**
   内容: 完善水池房间的各项功能，确保所有水池行动正常工作

6. **器官质量系统完善**
   内容: 完善器官质量相关的系统功能

7. **事件富文本系统**
   内容: 事件描述支持富文本效果，允许 mod 作者使用标签控制文本表现
     - 颜色、粗细、抖动等视觉效果
     - 声音联动
     - 自定义标签语法，如 `<抖动>恐~惧~吧~</抖动>`
     - 支持调用 Vue 组件渲染复杂效果

8. **卡牌选择奖励类型** ✅ 2026-07-29
   实现:
     - `src/core/objects/reward/CardSelectReward.ts` 新类，`type: "cardSelect"`
     - `initRewardRegistry.ts` 已注册
     - `RewardModal.vue` 加了内嵌卡牌选择弹窗（跟 relic 弹窗同结构）
     - claim() 走 doEvent gainCard，符合"效果通过事件触发"规则
   未做（等具体场景需要时再补）:
     - 卡牌池筛选辅助（稀有度/tag/来自哪个器官），目前由调用方自己提供 cardOptions
     - 卡片富渲染（当前只显 label + description，跟 relic 弹窗一致水平），等宝箱事件跑起来看效果再决定要不要用 `<Card>` 组件
   关联: 宝箱事件、多个新事件现在可用此奖励类型

### 中优先级

1. **主动使用系统完善**
   文件: `src/ui/hooks/interaction/abilityMenu.ts`
   待实现:
     - 实现能力详情弹窗 (第141行)
     - 实现不可用原因提示 (第149行)

2. **敌人行为系统完善**
   文件: `src/core/objects/system/EnemyBehavior.ts:169`
   内容: 实现 getStateStacks 方法

4. **奖励系统完善**
   文件:
     - `src/core/objects/reward/RelicReward.ts:57` - 将遗物添加到玩家
     - `src/core/objects/reward/GoldReward.ts:33` - 实现金钱系统后添加金钱
     - `src/core/objects/room/TreasureRoom.ts:127` - 等金钱系统实现后创建 GoldReward

### 低优先级

1. **黑市系统完善**
    待实现:
      - 器官/药水/卡牌也改用 drawItem + 栏位配置（目前仅遗物已改造）

3. **房间生成系统完善**
    文件:
      - `src/core/objects/room/roomChoice.ts:72` - 根据层级和概率规则生成房间
      - `src/core/objects/system/roomSelection/IncrementalProbabilityRule.ts:103` - 通过roomRegistry判断类型
      - `src/static/registry/initGameEventRegistry.ts:20` - 注册其他事件类型

4. **地图系统完善**
    文件:
      - `src/core/objects/room/FloorSelectRoom.ts:176` - 显示地图UI
      - `src/core/objects/system/FloorManager.ts:427,506` - 传入完整的Room对象历史
      - `src/core/objects/system/roomSelection/FloorSelectionState.ts:142,160` - 序列化和恢复动态规则配置

5. **机制系统完善**
    文件: `src/static/registry/mechanismRegistry.ts:355`
    内容: 持续回合数和自定义清零条件

6. **事件系统完善**
    文件:
      - `src/static/list/room/event/eventList.ts:111,145,156` - 打开卡牌/器官选择界面
      - `src/static/list/room/event/eventEffectMap.ts:24,32,370` - 实现材料和随机奖励

7. **意图系统完善**
    文件: `src/core/objects/system/Intent.ts:208`
    内容: 传入实际的目标，调整cardsToIntent参数

8. **层级配置完善**
    文件: `src/static/list/floor/floorList.ts:22,24`
    内容: 添加Boss战斗和黑市房间

9. **游戏统计系统**
    内容: 埋点收集本局游戏数据，用于失败/通关页面展示
      - 击杀敌人数、获得金币总数、使用卡牌次数、本局用时等
    相关: RunStats.ts, RunStatsPanel.vue 已搭好框架，等数据就位后扩展

10. **事件/触发器链 SMT 别名映射系统**
    背景: 在多层事件/触发器传递过程中，source/medium/target 的含义随每层变化，导致 mod 作者难以追踪"原始对象是谁"
    内容:
      - 方案A：在 trigger 层定义 smtAliases 别名映射
      - 方案B：扩展 targetType 增加 triggerEventSource/triggerEventTarget/triggerEventMedium

## 待讨论 (Pending Discussion)

- **黑市卡牌移除服务** — 付费从牌组移除卡牌，参考杀戮尖塔，机制待定
- **黑市对话系统** — 黑市老板在不同交互场景下有对话反馈（如买不起时辱骂玩家），需要对话系统支撑，当前先用 message 提示占位
- **复刻杀戮尖塔原版事件** — 有空时用现有事件系统复刻更多原版事件，验证系统完备性并补充第一层事件池。已完成换脸商(Face Trader) + 5 面具遗物，过程中扩展了 roomEnter/openChest 事件、$event.info() 条件语法、emptyChest/gainRelicFromPool 效果

## 文档整理

### 未归档文档 (待分类)

1. `主动使用系统.md` - 需要与代码同步
2. `卡牌引用系统.md` - 需要与代码同步
3. `效果预览系统.md` - 需要与代码同步
4. `器官解锁与精成系统.md` - 需要与代码同步
5. `器官解锁与精成系统-实现文档.md` - 需要与代码同步
6. `进阶系统.md` - 需要与代码同步

### 可归档文档

1. `机制UI系统使用指南.md`
2. `机制系统Mechanism.md`
3. `种子随机数系统.md`
4. `如何在新效果中使用随机数.md`
5. `discoverCard效果的随机数生成原理.md`
6. `黑市系统配置指南.md`
7. `器官稀有度与标签系统.md`
8. `器官质量系统与操作方法.md`
9. `器官奖励动作系统.md`
10. `器官奖励动作系统-快速参考.md`
11. `水池行动扩展系统.md`
12. `水池行动扩展系统-快速参考.md`

## 备忘事项

ifShow 目前是函数类型，未来如果使用场景增多，考虑改为 Condition 系统（需要解决 sceneData 引用命名问题）

进阶系统相关数值变体（待进阶系统实现后调整）:
- 金神像事件：砸烂 进阶15+ 失去35%生命（基础25%），躲藏 进阶15+ 失去10%最大生命（基础8%）

来自 `文档/备忘.md`:
- 【器官→脑】抽牌数量提升/牌效果为重抽or加抽/能够得知敌人的行为的强度
- 【器官→眼】能够获知敌人的行动/行动类型
- 根据交互的itemTarget判断选择目标的行为
- 词条也用触发器来做
- 核心问题：导致状态层数减少的对象就是它自己
- 状态对象问题：所有效果都要通过事件触发，否则不会进事件栈
- 战斗行为定义: 回合开始(抽牌、恢复能量)、回合结束(弃牌、耗光能量)
